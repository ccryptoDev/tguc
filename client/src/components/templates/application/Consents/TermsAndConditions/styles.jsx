import styled from "styled-components";

const Wrapper = styled.div`
  line-height: 1.5;
  ul {
    padding-left: 2rem;
  }
  p {
    margin: 1.6rem 0;
  }

  h2,
  h3 {
    font-weight: bold;
  }
`;

export default Wrapper;
