import styled from "styled-components";

const Wrapper = styled.div`
  line-height: 1.5;
  ul {
    padding-left: 20px;
  }
  p {
    margin: 16px 0;
  }

  h2,
  h3 {
    font-weight: bold;
  }
`;

export default Wrapper;
